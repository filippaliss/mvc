{% extends "base.html.twig" %}

{% block title %}ME{% endblock %}

{% block body %}
    <h1>API</h1>
    <table>
        <tr>
            <th>Link</th>
            <th>Description</th>
        </tr>
        <tr>
            <td><a href="{{url("api-qoute")}}">/api/quote</a></td>
            <td>A random quote and todays date</td>
        </tr>
        <tr>
            <td><a href="{{url("api-deck")}}">/api/deck</a></td>
            <td>A card deck</td>
        </tr>
        <tr>
            <td><a href="{{url("api-deck-shuffle")}}">/api/deck/shuffle</a></td>
            <td>Shuffleing the deck</td>
        </tr>
        <tr>
            <td>
                <form action="{{url('api-deck-draw')}}" method="POST">
                    <button type="submit">/api/deck/draw</button>
                </form>
            </td>
            <td>draw one card from the deck</td>
        </tr>
        <tr>
            <td>
                /api/deck/draw/{number}
            </td>
            <td>draw the number of cards you chooses from the deck</td>
        </tr>
        <tr>
            <td><a href="{{url('api_library_books')}}">api/library/books</a></td>
            <td>Get all books from the library in JSON format</td>
        </tr>
        <tr>
            <td><a href="{{url('api_library_book', {'isbn': '978-A001'})}}">api/library/book/{isbn}</a></td>
            <td>Get a specific book by ISBN number - example link provided</td>
        </tr>
     </table>
{% endblock %}
